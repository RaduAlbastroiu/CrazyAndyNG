import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  Image,
  useWindowDimensions,
} from 'react-native';
import React, {useState, useEffect} from 'react';

const TopSearch = ({route, navigation}) => {
  return (
    <View
      style={{
        borderColor: 'blue',
        borderWidth: 2,
      }}>
      <Text>TopSearch</Text>
    </View>
  );
};

export default TopSearch;
